version: "3.8"
services:
  deskbird-auto:
    build: .
    container_name: deskbird-auto
    ports:
      - "2713:2713"
    environment:
      - DESKBIRD_EMAIL=${DESKBIRD_EMAIL}
      - DESKBIRD_PASSWORD=${DESKBIRD_PASSWORD}
      - DESKBIRD_APP_KEY=${DESKBIRD_APP_KEY}
    restart: unless-stopped
